apiVersion: v1
kind: Service
metadata:
  name: qwen-ui-service
  namespace: qwen
  labels:
    app: qwen-ui
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 7860
      targetPort: 7860
      protocol: TCP
      name: gradio
  selector:
    app: qwen-ui
